\RequirePackage[cache=false]{minted}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% ソースコード貼り付けの設定
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newminted[texcode]{latex}{%
    linenos,
    frame=lines,
    numbersep=5pt,
    framesep=2mm,
    fontsize=\normalsize,
    breaklines,
}
\newmintedfile[inputtexcode]{latex}{%
    linenos,
    frame=lines,
    numbersep=5pt,
    framesep=2mm,
    fontsize=\normalsize,
    breaklines,
}
\newminted[bashcode]{bash}{%
    linenos,
    frame=lines,
    numbersep=5pt,
    framesep=2mm,
    fontsize=\normalsize,
    breaklines,
}
\newmintedfile[inputpython]{python}{%
    linenos,
    frame=lines,
    numbersep=5pt,
    framesep=2mm,
    fontsize=\normalsize,
    breaklines,
}
